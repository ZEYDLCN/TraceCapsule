<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\TraceCapsule.Core\TraceCapsule.Core.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <PropertyGroup>
    <Description>Bridges the .NET Activity graph (the same one OpenTelemetry exporters read from) into TraceCapsule's ambient capsule recording, with zero per-call-site instrumentation.</Description>
  </PropertyGroup>

</Project>
